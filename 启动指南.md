# Running Partners 项目启动指南

## 📋 前置条件检查

### ✅ 已完成
- [x] Node.js v22.13.1 已安装
- [x] npm v10.9.2 已安装
- [x] 代码已拉取到本地
- [x] 当前在 `feature/mvp-1` 分支

### ⚠️ 待完成
- [ ] 安装项目依赖
- [ ] 配置 Supabase
- [ ] 创建 .env 文件
- [ ] 运行数据库迁移

---

## 🚀 快速启动步骤

### 第1步：安装项目依赖

```bash
# 在项目根目录执行
npm install
```

**预计时间**: 2-5分钟  
**说明**: 这会安装所有 package.json 中列出的依赖包

---

### 第2步：配置 Supabase

#### 2.1 创建 Supabase 项目

1. 访问 [https://supabase.com](https://supabase.com)
2. 注册或登录账号
3. 点击 **"New Project"**
4. 填写项目信息：
   - **Project name**: `running-partners`
   - **Database password**: 设置一个强密码（记住它！）
   - **Region**: 选择 `Southeast Asia (Singapore)` 或最近的地区
5. 点击 **"Create new project"**
6. 等待 1-2 分钟，项目创建完成

#### 2.2 获取 API 凭证

1. 进入项目后，点击左侧 **Settings** (齿轮图标)
2. 选择 **API**
3. 复制以下两个值：
   - **Project URL**: 类似 `https://xxxxx.supabase.co`
   - **anon public key**: 以 `eyJ` 开头的长字符串

---

### 第3步：创建环境变量文件

在项目根目录创建 `.env` 文件：

```bash
# 创建 .env 文件
touch .env
```

然后编辑 `.env` 文件，填入你的 Supabase 凭证：

```env
EXPO_PUBLIC_SUPABASE_URL=https://你的项目ID.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=你的anon_key
```

**⚠️ 重要提示**：
- 不要把 `.env` 文件提交到 Git
- 不要泄露你的 API Key
- `.gitignore` 已经包含了 `.env`，所以默认不会被提交

---

### 第4步：运行数据库迁移

#### 4.1 在 Supabase 控制台执行 SQL

1. 在 Supabase 项目中，点击左侧 **SQL Editor**
2. 点击 **"New Query"** 按钮

#### 4.2 依次执行以下迁移文件

**顺序很重要！必须按照编号顺序执行：**

##### ① 创建用户表
```bash
# 文件: backend/supabase/sql/migrations/001_create_users_table.sql
```
复制该文件内容到 SQL Editor → 点击 **Run**

##### ② 创建跑步者位置表
```bash
# 文件: backend/supabase/sql/migrations/002_create_runners_table.sql
```
复制该文件内容到 SQL Editor → 点击 **Run**

##### ③ 添加用户名唯一约束
```bash
# 文件: backend/supabase/sql/migrations/003_add_unique_username_constraint.sql
```

##### ④ 添加跑步者user_id唯一约束
```bash
# 文件: backend/supabase/sql/migrations/004_add_unique_runner_user_id_constraint.sql
```

##### ⑤ 创建新用户触发器
```bash
# 文件: backend/supabase/sql/migrations/005_create_handle_new_user_trigger.sql
```

##### ⑥ 添加级联删除
```bash
# 文件: backend/supabase/sql/migrations/006_add_users_auth_fk_cascade.sql
```

##### ⑦ 启用实时订阅 ⭐重要
```bash
# 文件: backend/supabase/sql/migrations/007_enable_realtime_runners.sql
```

##### ⑧ 创建跑步邀请表
```bash
# 文件: backend/supabase/sql/migrations/008_create_run_invites_table.sql
```

##### ⑨ 修复RLS策略
```bash
# 文件: backend/supabase/sql/migrations/009_fix_runners_self_select_policy.sql
```

##### ⑩ 添加路线字段
```bash
# 文件: backend/supabase/sql/migrations/010_add_route_to_run_invites.sql
```

##### ⑪ 添加调查字段
```bash
# 文件: backend/supabase/sql/migrations/011_add_survey_fields_to_users.sql
```

##### ⑫ 添加跑步历史字段
```bash
# 文件: backend/supabase/sql/migrations/012_add_run_history_fields.sql
```

#### 4.3 验证迁移成功

在 Supabase 控制台：
1. 点击左侧 **Database** → **Tables**
2. 你应该能看到：
   - `users` 表
   - `runners` 表
   - `run_invites` 表

---

### 第5步：启动开发服务器

```bash
# 启动 Expo 开发服务器
npm start
```

**你会看到**：
```
› Metro waiting on exp://192.168.x.x:8081
› Scan the QR code above with Expo Go (Android) or the Camera app (iOS)

› Press a │ open Android
› Press i │ open iOS simulator
› Press w │ open web

› Press j │ open debugger
› Press r │ reload app
› Press m │ toggle menu
› Press o │ open project code in your editor

› Press ? │ show all commands
```

---

## 📱 在不同平台运行

### Web 浏览器（最简单）

```bash
npm run web
```

会自动打开 `http://localhost:8081` 在浏览器中

**优点**：
- 不需要额外软件
- 调试方便
- 热重载快

**缺点**：
- GPS模拟不真实
- 某些原生功能不可用

---

### iOS 模拟器（需要 macOS + Xcode）

```bash
npm run ios
```

**前置条件**：
1. 安装 Xcode：从 App Store 下载
2. 安装 Xcode Command Line Tools：
   ```bash
   xcode-select --install
   ```
3. 启动 Xcode 至少一次（接受许可协议）

---

### Android 模拟器（需要 Android Studio）

```bash
npm run android
```

**前置条件**：
1. 安装 Android Studio
2. 创建虚拟设备 (AVD)
3. 安装 Android SDK

---

### 真实手机（推荐测试 GPS）

#### 方法1: 使用 Expo Go App

1. 手机安装 **Expo Go**：
   - iOS: App Store 搜索 "Expo Go"
   - Android: Google Play 搜索 "Expo Go"

2. 确保手机和电脑在同一 WiFi 网络

3. 运行 `npm start`

4. 用手机扫描终端显示的二维码

#### 方法2: 开发构建（Development Build）

更接近生产环境，但配置更复杂（暂时不需要）

---

## 🐛 常见问题排查

### 问题1: npm install 失败

**错误信息**：
```
npm ERR! code ERESOLVE
npm ERR! ERESOLVE unable to resolve dependency tree
```

**解决方案**：
```bash
# 清理缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

---

### 问题2: Expo 启动失败

**错误信息**：
```
Error: Cannot find module 'expo'
```

**解决方案**：
```bash
# 确保依赖已安装
npm install

# 如果还是不行，全局安装 Expo CLI
npm install -g expo-cli

# 或者使用 npx
npx expo start
```

---

### 问题3: Supabase 连接失败

**错误信息**：
```
Failed to fetch
或
Invalid API key
```

**检查清单**：
- [ ] `.env` 文件是否存在于项目根目录？
- [ ] `EXPO_PUBLIC_SUPABASE_URL` 格式正确吗？（https://xxx.supabase.co）
- [ ] `EXPO_PUBLIC_SUPABASE_ANON_KEY` 是完整的吗？
- [ ] 重启开发服务器了吗？（修改 .env 后必须重启）

**验证方法**：
```bash
# 查看环境变量是否加载
cat .env

# 重启服务器
# Ctrl + C 停止
npm start
```

---

### 问题4: 数据库表不存在

**错误信息**：
```
relation "public.users" does not exist
```

**解决方案**：
- 确认所有 SQL 迁移文件都已执行
- 按照顺序执行（001 → 002 → ... → 012）
- 在 Supabase 控制台检查表是否创建成功

---

### 问题5: GPS 权限被拒绝

**错误信息**：
```
Location permission was denied
```

**解决方案**：

**iOS 模拟器**：
1. Features → Location → Custom Location
2. 输入新加坡坐标：`1.3521, 103.8198`

**Android 模拟器**：
1. Extended Controls (...)
2. Location → Latitude: 1.3521, Longitude: 103.8198

**真实手机**：
1. 设置 → Running Partners → 位置权限 → 始终允许

---

### 问题6: 实时更新不工作

**现象**：
- 其他用户上线/下线时，地图没有更新
- 邀请发送后，对方收不到通知

**检查清单**：
- [ ] 执行了 `007_enable_realtime_runners.sql` 吗？
- [ ] 在 Supabase 控制台确认 Realtime 已启用：
  1. Database → Replication
  2. 确认 `runners` 和 `run_invites` 表已启用复制

**解决方案**：
```sql
-- 在 SQL Editor 执行
ALTER TABLE runners REPLICA IDENTITY FULL;
ALTER TABLE run_invites REPLICA IDENTITY FULL;

-- 确认启用
SELECT schemaname, tablename, relreplident 
FROM pg_class 
JOIN pg_namespace ON pg_namespace.oid = pg_class.relnamespace
WHERE relname IN ('runners', 'run_invites');
```

---

## 🧪 测试功能

### 测试1: 注册登录

1. 启动应用
2. 点击 "SIGN UP"
3. 填写：
   - Email: test@example.com
   - Password: Test123456
   - Username: TestUser
4. 点击注册
5. 填写入职调查（随便填）
6. 成功进入主界面

### 测试2: 查看地图

1. 允许位置权限
2. 地图应该显示你的当前位置（蓝色圆点）
3. 地图应该以你为中心

### 测试3: 上线状态

1. 点击 "GO ONLINE" 按钮
2. 按钮变为黄绿色 "FINDING PARTNERS"
3. 打开 Supabase 控制台 → Table Editor → runners
4. 应该能看到你的记录，`is_active = true`

### 测试4: 多用户测试（需要两个设备）

**设备1**：
1. 注册账号 User1
2. 点击上线
3. 位置应该显示在地图上

**设备2**：
1. 注册账号 User2
2. 点击上线
3. 应该能在地图上看到 User1 的标记

**测试实时更新**：
- User1 移动 → User2 的地图上应该看到 User1 的标记移动
- User1 下线 → User2 的地图上 User1 的标记应该消失

---

## 📊 项目结构速览

```
Running-Partners/
├── App.tsx                      # 根组件
├── package.json                 # 依赖配置
├── .env                         # 环境变量（需创建）
│
├── frontend/
│   ├── app/
│   │   └── AuthContext.tsx      # 认证上下文
│   ├── components/              # UI组件
│   │   ├── LeafletMap.tsx       # 地图组件
│   │   ├── OnlineToggleButton.tsx
│   │   └── ...
│   ├── screens/                 # 页面
│   │   ├── AuthScreen.tsx       # 登录页
│   │   ├── OnboardingScreen.tsx # 入职调查
│   │   ├── HomeScreen.tsx       # 主界面⭐
│   │   └── ProfileScreen.tsx    # 个人主页
│   ├── hooks/
│   │   ├── useAuth.ts           # 认证Hook
│   │   └── useLocation.ts       # 位置Hook⭐
│   ├── services/
│   │   ├── supabaseClient.ts    # Supabase客户端
│   │   ├── authService.ts       # 认证服务
│   │   └── runService.ts        # 跑步服务
│   └── types/
│       └── index.ts             # 类型定义
│
└── backend/
    └── supabase/
        └── sql/migrations/      # 数据库迁移⭐
            ├── 001_create_users_table.sql
            ├── 002_create_runners_table.sql
            └── ...
```

---

## 🎯 下一步

完成启动后，你可以：

1. **阅读代码**：从 `HomeScreen.tsx` 开始
2. **测试功能**：尝试注册、上线、发送邀请
3. **查看数据**：在 Supabase 控制台查看数据库记录
4. **学习实时功能**：观察 Realtime 如何工作
5. **修改代码**：尝试改变主题颜色、调整地图样式

---

## 📚 有用的链接

- **Expo 文档**: https://docs.expo.dev/
- **React Native 文档**: https://reactnative.dev/
- **Supabase 文档**: https://supabase.com/docs
- **Leaflet 文档**: https://leafletjs.com/
- **项目分析报告**: `./分支分析报告.md`

---

## 🆘 需要帮助？

如果遇到问题：
1. 先查看本文档的"常见问题排查"部分
2. 检查终端的错误信息
3. 查看浏览器控制台（F12）的错误
4. 询问团队成员或查阅文档

---

**祝你启动顺利！🚀**

生成时间: 2026年8月3日